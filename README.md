# gestao-empresarial-rental-retros
Sistema App para genrenciamento dos departamentos da empresa
## Banco de dados local

Durante o desenvolvimento local, o projeto usa SQLite através da variável `DATABASE_URL`.

Exemplo no `.env`:

```env
DATABASE_URL=sqlite:///desenvolvimento.db
